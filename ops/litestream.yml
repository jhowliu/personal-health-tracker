# 備份目的地必須在主機以外,否則主機掛掉就一起沒了。
dbs:
  - path: /data/pht.sqlite
    replicas:
      - type: s3
        endpoint: ${BACKUP_S3_ENDPOINT}
        bucket: ${BACKUP_S3_BUCKET}
        path: pht
        access-key-id: ${BACKUP_S3_ACCESS_KEY}
        secret-access-key: ${BACKUP_S3_SECRET_KEY}
        retention: 720h
        sync-interval: 10s
